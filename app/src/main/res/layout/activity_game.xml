<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Main Content Layout -->
    <RelativeLayout
        android:id="@+id/mainContent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        android:background="@color/background_color">

        <!-- First Button Row: Cow, Church, Water Tower -->
        <LinearLayout
            android:id="@+id/buttonRow"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_alignParentTop="true"
            android:layout_marginTop="16dp">

            <!-- Cow Button -->
            <Button
                android:id="@+id/cowButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="Cow (1)"
                android:textSize="18sp"
                android:layout_weight="1"
                android:layout_margin="8dp" />

            <!-- Church Button -->
            <Button
                android:id="@+id/churchButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="Church (2)"
                android:textSize="18sp"
                android:layout_weight="1"
                android:layout_margin="8dp" />

            <!-- Water Tower Button -->
            <Button
                android:id="@+id/waterTowerButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="Water Tower (3)"
                android:textSize="18sp"
                android:layout_weight="1"
                android:layout_margin="8dp" />
        </LinearLayout>

        <!-- White Fence Button (Single Button Row) -->
        <Button
            android:id="@+id/whiteFenceButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="White Fence"
            android:textSize="18sp"
            android:layout_below="@id/buttonRow"
            android:layout_marginTop="16dp"
            android:layout_margin="8dp" />

        <!-- Team Points TextView -->
        <TextView
            android:id="@+id/teamPointsTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Team Points: 0"
            android:textSize="18sp"
            android:layout_below="@id/whiteFenceButton"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="16dp" />

        <!-- Button to Open Drawer (Replace 'Players' button) -->
        <Button
            android:id="@+id/playerButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Who's Playing"
            android:layout_alignParentEnd="true"
            android:layout_margin="16dp"
            android:layout_below="@id/teamPointsTextView"
            android:layout_marginTop="16dp" />

        <!-- Back Button (Bottom Left) -->
        <Button
            android:id="@+id/backButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Back"
            android:layout_alignParentBottom="true"
            android:layout_alignParentStart="true"
            android:layout_margin="16dp" />

    </RelativeLayout>

    <!-- Right-side Navigation Drawer -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:fitsSystemWindows="true">

        <!-- Drawer Header (Optional) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/drawerHeaderText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Players"
                android:textSize="24sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- RecyclerView for Players -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/drawerRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp" />
    </com.google.android.material.navigation.NavigationView>
</androidx.drawerlayout.widget.DrawerLayout>
