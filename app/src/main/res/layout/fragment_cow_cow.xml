<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_landscape"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".gameFragments.CowCowFragment">

    <!-- Player Score Display -->
    <TextView
        android:id="@+id/playerScoreTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total Score: 0"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Cow Button -->
    <Button
        android:id="@+id/cowButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cow"
        app:layout_constraintTop_toBottomOf="@id/playerScoreTextView"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Church Button -->
    <Button
        android:id="@+id/churchButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Church"
        app:layout_constraintTop_toBottomOf="@id/playerScoreTextView"
        app:layout_constraintStart_toEndOf="@id/cowButton" />

    <!-- Water Tower Button -->
    <Button
        android:id="@+id/waterTowerButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Water Tower"
        app:layout_constraintTop_toBottomOf="@id/playerScoreTextView"
        app:layout_constraintStart_toEndOf="@id/churchButton" />

    <!-- Reset Button -->
    <Button
        android:id="@+id/resetButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Reset"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
